# uses streams.exe from sysinternals https://docs.microsoft.com/en-us/sysinternals/downloads/streams
# this removes the Zone data that blocks untrusted files from being run and Unblock-Files doesn't work. Use with caution.
# run powershell.exe as admin
# no switches will show the zone data
# -d will strip zone data, -s will recursively move through the dir structure

.\streams -s -d <path>\
